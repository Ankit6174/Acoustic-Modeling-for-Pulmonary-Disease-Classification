[project]
name = "acoustic-modeling-for-pulmonary-disease-classification"
version = "0.1.0"
description = "Machine learning and deep learning pipeline to train and deploy pulmonary disease detection model."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "librosa>=0.11.0",
]
